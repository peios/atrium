[package]
name = "atrium-http"
description = "The least HTTP/1.1 Atrium's server and session hosts need, over any stream."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde_json = { workspace = true }
